Sources for the plugin `plugin.orientation`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.orientation" = {
            publisherId = "tech.scotth",
        },
    },
}
```
